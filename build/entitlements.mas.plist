<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements for the Mac App Store build.

  MAS forces App Sandbox on. The vault folder is reachable via
  `user-selected.read-write` once the user picks it, but to *keep* access
  across launches you must save a **security-scoped bookmark** (see
  PUBLISHING.md). The other entitlements mirror what hardened runtime needs
  for V8 + native modules.
-->
<plist version="1.0">
<dict>
  <!-- App Sandbox is mandatory for MAS. -->
  <key>com.apple.security.app-sandbox</key>
  <true/>

  <!-- V8 JIT. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- The user picks the vault folder; we then resolve a security-scoped
       bookmark on every launch to keep access. -->
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
  <key>com.apple.security.files.bookmarks.app-scope</key>
  <true/>

  <!-- Microphone access for voice dictation (App Sandbox key). -->
  <key>com.apple.security.device.microphone</key>
  <true/>

  <!-- Outbound networking — we don't need it, but Electron's networking
       stack still initializes; leaving this off is safer and tells the
       reviewer the app is fully offline. Re-enable if you ever add sync. -->
  <!--
  <key>com.apple.security.network.client</key>
  <true/>
  -->
</dict>
</plist>
